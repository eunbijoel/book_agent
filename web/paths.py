from __future__ import annotations

from pathlib import Path

# book_agent/ (project root)
PROJECT_ROOT = Path(__file__).resolve().parent.parent

OUTPUTS_DIR = PROJECT_ROOT / "outputs"
TOC_DIR = PROJECT_ROOT / "toc"
